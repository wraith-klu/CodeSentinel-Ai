# 🧠 Code Smell Detection Model (FastAI)

This folder contains the FastAI-based model used for detecting code smells.

## Files
- `train_model.ipynb`: Notebook for training and exporting the model.
- `export.pkl`: Trained model file (generated after running the notebook).
- `model_loader.py`: Loads model and predicts code smell.
